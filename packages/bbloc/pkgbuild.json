{
    "package": "bbloc",
    "info": {
        "title": "bbloc",
        "author": "Romain337",
        "category": "tool",
        "version": "alpha_6",
        "url": "https://github.com/notnotme/bbloc/tree/nintendo_switch",
        "license": "n/a",
        "description": "A text and code editor.",
        "details": "A text editor which work with a command prompt.\\n\\nYou open the prompt by using a keystroke (\"ctrl+shift+space\", with AZERTY layout), and type the command you want, like \"open [file_path]\", \"save [file_path]\". You can use the \"tab\" key to auto-complete commands and most arguments.\\n\\nThe UI tints and dimension can be altered by modifying variables using the prompt.\\n\\nWhen the prompt is not open, the text goes into the buffer.\\n\\nPlease check the README file on github to know how it work in details. \\n\\n"
    },
    "assets": [
        {
            "type": "icon",
            "url": "icon.png"
        },
        {
            "type": "screenshot",
            "url": "capture.png"
        },
        {
            "type": "update",
            "url": "https://github.com/notnotme/bbloc/releases/download/alpha_6/bbloc.nro",
            "dest": "/switch/bbloc/bbloc.nro"
        }
    ],
    "changelog": "valpha_6\\nWhat's new\\n\\n   Added\\n  - Multiple open buffers- — each file gets its own buffer; switch with the new buffer command (next / prev / close /  with Tab completion), bound to Ctrl+Tab, Ctrl+Shift+Tab and Ctrl+W. The top bar shows the buffer position.\\n  - Search and replace- — find next/previous and replace, with a match counter in the prompt.\\n  - Editor scrollbars- — proportional vertical and horizontal bars, shown only when content overflows. Toggleable with the show_scrollbar cvar; horizontal mouse wheel scrolling now works too.\\n  - goto_line command- to jump to a line.\\n  - Open a file from the command line: bbloc [file].\\n  - INI syntax highlighting- alongside C++ and JSON.\\n  - Better keyboard support- — improved USB keyboard handling with support for more keyboard layouts/languages (via a custom SDL2 patch on top of devkitPro's).\\n\\n   Changed\\n  - Refined command system with richer interactive feedback (confirmations, argument prompts with Tab completion).\\n  - No more hard quad budget: rendering batches are staged CPU-side and the GPU buffer grows on demand, so the UI can never run out of quads.\\n  - Long file names are ellipsized in the top bar.\\n\\n   Fixed\\n  - Crash fixes: use-after-free when closing a buffer, clipboard paste aborting on non-UTF-8 selections, stale selection anchor running past the end of the buffer, and large font sizes aborting at startup.\\n  - Saving is now atomic (write to a temp file, then rename) — a failed write no longer truncates the file.\\n  - Backward search can now match on the first line, and vertical moves no longer split surrogate pairs (which could make the buffer unsaveable).\\n  - Big performance work: linear-time multi-line paste (10k lines: 637 ms → 5.5 ms), shared tree-sitter queries across buffers, smarter highlight caching, and cheaper undo/redo and search stepping.\\n\\nDon't forget, this is not yet by default:\\nLight and dark themes- — switch at any time with Ctrl+Shift+L (light) or Ctrl+Shift+D (dark).\\n\\nvalpha_5\\nChanges:\\n- Added Undo (Ctrl+Z) and Redo (Ctrl+Shift+Z) features.\\n- Added light (\"Ctrl+Shift+L\") and dark (\"Ctrl+Shift+D\") theme.\\n- Changed text storage buffer from VectorBuffer to LineBuffer.\\n- Reworked command flow (ex: Cut/Copy/Paste working from prompt).\\n- Proper syntax highlight (JSON, CPP).\\n- Better auto complete for path and arguments.\\n- General system stability improvements to enhance the user's experience.\\n\\nNote:\\n- Light or dark theme are not by default. You must select one of theme manually.\\n- When auto completing a path, to enter a listed folder, type \"/\", then auto complete will list what is inside that folder.\\n\\nPlanned:\\n- More syntax highlight (INI, YAML, ???).\\n- Automatic theme selection according to user switch theme will comes in later release.\\n\\nLight\\n\\n\\nDark\\n\\nvalpha_4\\n- fix update logic (refresh only when necessary on all platforms)\\n- fix wrong mapping bol -> bof\\n- fix wrong mapping bof <-> eof\\n- cursor column position stick to column when possible\\n\\nvalpha_3\\nLatest build including all refactoring.\\n\\nRelease \"0\" pre-alpha.\\n\\nRight now I cannot work more on it, as I need to rampup in my new work, and it will take some times. But I like this project and will update it later when free times will allow it."
}